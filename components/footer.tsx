import React from 'react';
import styles from '../styles/footer.module.css'; // Use CSS Modules

const Footer = () => {
  return (
    <div className={styles.Footer}>
      <div className={styles.FooterContent}>
        <p>Footer</p>
      </div>
    </div>
  );
};

export default Footer;
